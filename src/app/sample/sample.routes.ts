import { Routes, } from '@angular/router';

import { ControlsComponent } from './controls/controls.component';

export const sampleComponentsRoutes: Routes = [
    { path: 'controls', component: ControlsComponent }
];
