import { DateByPeriodPipe } from './date-by-period.pipe';

describe('DateByPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new DateByPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
